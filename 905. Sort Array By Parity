class Solution {
public:
    vector<int> sortArrayByParity(vector<int>& nums) {
    vector<int> k;
    for(auto x:nums)
    {
        if(x%2==0)
        k.push_back(x);
    }
    for(auto x:nums)
    {
        if(x%2!=0)
        k.push_back(x);
    }
    return k;
    }
};
