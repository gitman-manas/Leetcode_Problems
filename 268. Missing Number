class Solution {
public:
    int missingNumber(vector<int>& nums) {
     int n=nums.size(),s=0,k;
    for(int i=0;i<n;i++)
    {s+=nums[i];}
        k=(n*(n+1))/2;
        return k-s;
    } 
        
};
