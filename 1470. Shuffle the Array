class Solution {
public:
    vector<int> shuffle(vector<int>& nums, int n) {
    vector<int> a;
    int i=0;
    int j=nums.size()/2;
    while(i<nums.size()/2)
    {
        a.push_back(nums[i++]);
        a.push_back(nums[j++]);
    }
    return a;
    }
};
